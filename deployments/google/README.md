# Google Cloud deployment

Reserved for Milestones 1–3. Google Cloud is the only POC cloud. The target must be dual-stack IPv4/IPv6, expose application traffic through TLS on TCP 443, and keep PostgreSQL and AI runtime ports private.
