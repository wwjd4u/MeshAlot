import React,{useEffect,useState}from"react";import{createRoot}from"react-dom/client";import"./style.css";
type Node={node_id:string,status:string,mode:string,agent_version:string};const api=import.meta.env.VITE_API_BASE_URL??"http://127.0.0.1:8080";
function App(){const[nodes,setNodes]=useState<Node[]>([]);const[error,setError]=useState("");useEffect(()=>{fetch(`${api}/v1/nodes`).then(r=>{if(!r.ok)throw new Error(r.statusText);return r.json()}).then(d=>setNodes(d.nodes)).catch(e=>setError(e.message))},[]);return <main><p className="eyebrow">MESHALOT POC</p><h1>Distributed compute, measured honestly.</h1><section><h2>Nodes</h2>{error&&<p className="error">API unavailable: {error}</p>}{!error&&!nodes.length&&<p>No enrolled nodes yet.</p>}{nodes.map(n=><article key={n.node_id}><strong>{n.node_id}</strong><span>{n.status} · {n.mode} · agent {n.agent_version}</span></article>)}</section></main>}createRoot(document.getElementById("root")!).render(<React.StrictMode><App/></React.StrictMode>);
